import { Container } from 'components/Container/Container.styled';
import { FooterBlock, FooterTitle } from './Footer.styled';
export const Footer = () => {
  return (
    <Container>
      <FooterBlock>
        <FooterTitle>
          We love our work, so we continually strive for excellence
        </FooterTitle>
      </FooterBlock>
    </Container>
  );
};
